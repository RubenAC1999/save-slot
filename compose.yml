services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: saveslot
      POSTGRES_USER: saveslot
      POSTGRES_PASSWORD: saveslot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
